# Tortex OTT — Login Fixed

## Admin Login
Username: `Rohan`
Password: `Rohan@5566`

The admin login is now attached with JavaScript using an element ID instead of inline HTML, and displays a clear error message for wrong credentials.

### GitHub upload
Replace the existing repository files with the files in this ZIP, preserving:
- `index.html`
- `style.css`
- `script.js`
- `tortex-logo.png`
- `admin/`

Open `/admin/` for the admin panel.

If GitHub Pages still shows the old login screen after replacing files, do a hard refresh/clear site data because the browser may have cached the old JavaScript.
